# Base (Fase 0-2): lectura HDF5, metadata, resampleo, conversion WFDB->HDF5
h5py==3.16.0
numpy==2.5.1
pandas==3.0.5
pyarrow==25.0.0
scipy==1.18.0
wfdb==4.3.1
tqdm==4.70.0

# EDA (Fase 1)
matplotlib==3.11.1
jupyterlab

# Modelado (Fase 4)
# torch se instala desde el indice de PyTorch, no desde PyPI, porque la rueda de PyPI
# viene sin CUDA. Para GPU NVIDIA:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
# (ajustar cu126 a la version de CUDA del driver; sin GPU, pip install torch a secas).
torch==2.13.0
scikit-learn==1.9.0